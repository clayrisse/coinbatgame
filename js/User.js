'use strict';


//cl:instead of having a function, we'll have a class (is almost the same)
class User {   
    constructor(name, email, password) {
        this.name = name;  
        this.email = email;
        this.password = password;
        
    }

}
